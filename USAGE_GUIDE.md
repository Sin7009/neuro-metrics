# Руководство по использованию neuro-metrics

## Простая "продажа" библиотеки

**neuro-metrics** - это библиотека для тех, кто хочет создавать красивые графики, **не изучая Python глубоко**.

## 🎯 Для кого эта библиотека?

- Аналитики, которые работают с данными в Excel/CSV
- Менеджеры, которым нужны презентабельные графики
- Все, кто хочет визуализировать данные одной строкой кода

## 🚀 Три шага к красивому графику

### Шаг 1: Установка (один раз)

```bash
pip install neuro-metrics
```

### Шаг 2: Импорт (в начале скрипта)

```python
import neuro_metrics as nm
import pandas as pd
```

### Шаг 3: Создание графика (одна строка!)

```python
# Если у вас есть CSV файл с данными
df = pd.read_csv('продажи.csv')
nm.plot_line(df, x='месяц', y='продажи', title='Продажи по месяцам')
```

## 📊 Готовые рецепты

### Рецепт 1: График продаж из словаря

```python
import neuro_metrics as nm

# Ваши данные (можно скопировать из Excel)
продажи = {
    'Январь': 100,
    'Февраль': 120,
    'Март': 115,
    'Апрель': 140
}

# Создать график
nm.plot_bar(продажи, title='Продажи в 2024')
```

### Рецепт 2: График из Excel файла

```python
import neuro_metrics as nm
import pandas as pd

# Загрузить данные из Excel
df = pd.read_excel('данные.xlsx')

# Создать линейный график
nm.plot_line(df, x='дата', y='выручка', 
             title='Выручка по датам',
             save_path='график.png')  # Сохранить в файл
```

### Рецепт 3: Круговая диаграмма долей

```python
import neuro_metrics as nm

# Доли рынка
доли = {
    'Продукт А': 35,
    'Продукт Б': 25,
    'Продукт В': 20,
    'Прочие': 20
}

# Создать круговую диаграмму
nm.plot_pie(доли, title='Распределение долей рынка')
```

### Рецепт 4: Сравнение категорий

```python
import neuro_metrics as nm

# Данные для сравнения
рейтинг = {
    'Москва': 850,
    'Санкт-Петербург': 620,
    'Казань': 380,
    'Новосибирск': 340
}

# Горизонтальная диаграмма
nm.plot_bar(рейтинг, horizontal=True,
            title='Продажи по регионам',
            xlabel='Продажи (млн руб)')
```

### Рецепт 5: Несколько линий на одном графике

```python
import neuro_metrics as nm
import pandas as pd

# Данные с несколькими показателями
df = pd.DataFrame({
    'квартал': ['Q1', 'Q2', 'Q3', 'Q4'],
    'выручка': [1200, 1450, 1380, 1650],
    'прибыль': [240, 290, 276, 330],
    'затраты': [960, 1160, 1104, 1320]
})

# График с тремя линиями
nm.plot_line(df, x='квартал', y=['выручка', 'прибыль', 'затраты'],
             title='Финансовые показатели 2024')
```

### Рецепт 6: Корреляционная матрица

```python
import neuro_metrics as nm
import pandas as pd

# Загрузить данные
df = pd.read_csv('метрики.csv')

# Вычислить корреляции
корреляции = df.corr()

# Создать тепловую карту
nm.plot_heatmap(корреляции, title='Корреляция показателей',
                annot=True, fmt='.2f')
```

## 🎨 Автоматический стиль Сбера

Все графики автоматически используют:
- ✅ Корпоративные цвета Сбера (зеленый, синий, оранжевый, и т.д.)
- ✅ Профессиональное оформление
- ✅ Читаемые шрифты и размеры
- ✅ Правильные отступы и пропорции

**Вам не нужно ничего настраивать!**

## 💾 Сохранение графиков

Добавьте параметр `save_path` для сохранения:

```python
nm.plot_line(df, x='x', y='y', 
             title='Мой график',
             save_path='мой_график.png')
```

График сохранится в высоком разрешении (300 DPI) - готов для презентаций!

## 📚 Полный список функций

| Функция | Что создает | Пример использования |
|---------|-------------|---------------------|
| `plot_line()` | Линейный график | Тренды, временные ряды |
| `plot_bar()` | Столбчатая диаграмма | Сравнение категорий |
| `plot_scatter()` | Точечная диаграмма | Корреляции, распределения |
| `plot_pie()` | Круговая диаграмма | Доли, пропорции |
| `plot_heatmap()` | Тепловая карта | Корреляции, матрицы |
| `plot_histogram()` | Гистограмма | Распределения значений |

## ❓ Частые вопросы

**В: Нужно ли мне знать Python?**  
О: Нет! Достаточно уметь копировать и изменять примеры.

**В: Как загрузить данные из Excel?**  
О: `df = pd.read_excel('файл.xlsx')`

**В: Как загрузить данные из CSV?**  
О: `df = pd.read_csv('файл.csv')`

**В: Можно ли изменить цвета?**  
О: Да, добавьте параметр `colors=['#FF0000', '#00FF00']`

**В: Как сделать график больше?**  
О: Добавьте параметр `figsize=(12, 8)` (ширина, высота в дюймах)

## 🎓 Начните прямо сейчас

1. Установите: `pip install neuro-metrics`
2. Скопируйте один из рецептов выше
3. Замените данные на свои
4. Запустите скрипт!

## 📧 Поддержка

Если что-то не работает - создайте issue на GitHub или обратитесь к документации в README.md.

---

**Библиотека создана для того, чтобы делать вашу работу проще, а результат - красивее! 🎨**
