# neuro-metrics

**Профессиональная библиотека для визуализации данных с корпоративным стилем Сбера**

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![uv](https://img.shields.io/badge/managed%20by-uv-green)](https://github.com/astral-sh/uv)

## 🎨 Ключевые особенности

- **Один вызов функции** - создавайте профессиональные графики одной строкой кода
- **Корпоративные цвета Сбера** - встроенная палитра с брендовыми цветами
- **Готовые решения** - не нужно изучать Python глубоко, просто используйте функции
- **Современные инструменты** - управление зависимостями через `uv`

## 📦 Установка

### С помощью uv (рекомендуется)

```bash
# Установите uv, если еще не установлен
curl -LsSf https://astral.sh/uv/install.sh | sh

# Добавьте библиотеку в ваш проект
uv add neuro-metrics
```

### С помощью pip

```bash
pip install neuro-metrics
```

## 🚀 Быстрый старт

```python
import neuro_metrics as nm
import pandas as pd

# Пример 1: Линейный график
df = pd.DataFrame({
    'month': ['Янв', 'Фев', 'Мар', 'Апр', 'Май', 'Июн'],
    'sales': [100, 120, 115, 140, 150, 145]
})
nm.plot_line(df, x='month', y='sales', title='Продажи по месяцам')

# Пример 2: Столбчатая диаграмма
data = {'Продукт А': 30, 'Продукт Б': 45, 'Продукт В': 25}
nm.plot_bar(data, title='Распределение продаж')

# Пример 3: Круговая диаграмма
nm.plot_pie(data, title='Доля рынка')
```

## 📊 Доступные функции

### 1. Линейные графики (`plot_line`)

Идеально для отображения трендов и временных рядов.

```python
# Простой линейный график
nm.plot_line(df, x='date', y='value', title='Тренд продаж')

# Несколько линий
nm.plot_line(df, x='date', y=['sales', 'costs'], 
             title='Продажи и затраты',
             xlabel='Дата', ylabel='Сумма (тыс. руб)')

# Сохранить график
nm.plot_line(df, x='date', y='value', 
             save_path='output.png')
```

### 2. Столбчатые диаграммы (`plot_bar`)

Для сравнения категорий и значений.

```python
# Вертикальная столбчатая диаграмма
nm.plot_bar(data_dict, title='Продажи по категориям')

# Горизонтальная
nm.plot_bar(data_dict, horizontal=True, 
            title='Рейтинг продуктов')

# Группированные столбцы
nm.plot_bar(df, x='category', y=['q1', 'q2', 'q3'], 
            title='Продажи по кварталам')
```

### 3. Точечные диаграммы (`plot_scatter`)

Для анализа корреляций и распределений.

```python
# Простая точечная диаграмма
nm.plot_scatter(df, x='price', y='sales', 
                title='Зависимость продаж от цены')

# С группировкой по категориям
nm.plot_scatter(df, x='price', y='sales', hue='category',
                title='Корреляция с категориями')

# С регулировкой размера точек
nm.plot_scatter(df, x='x', y='y', size=100, alpha=0.6)
```

### 4. Тепловые карты (`plot_heatmap`)

Для визуализации матриц и корреляций.

```python
# Корреляционная матрица
corr_matrix = df.corr()
nm.plot_heatmap(corr_matrix, title='Корреляционная матрица',
                annot=True, fmt='.2f')

# Кастомная тепловая карта
nm.plot_heatmap(data_matrix, cmap='Greens', 
                title='Интенсивность по регионам')
```

### 5. Круговые диаграммы (`plot_pie`)

Для отображения долей и пропорций.

```python
# Простая круговая диаграмма
nm.plot_pie(data_dict, title='Распределение долей')

# С выделением сегмента
nm.plot_pie(data_dict, explode=[0, 0.1, 0, 0],
            title='Фокус на втором сегменте')
```

### 6. Гистограммы (`plot_histogram`)

Для анализа распределений.

```python
# Гистограмма с KDE
nm.plot_histogram(data_array, bins=30, kde=True,
                  title='Распределение оценок')

# Простая гистограмма
nm.plot_histogram(data_array, bins=20, kde=False,
                  xlabel='Значение', ylabel='Частота')
```

## 🎨 Работа с цветами

Библиотека включает встроенную палитру корпоративных цветов Сбера:

```python
# Использование отдельных цветов
from neuro_metrics import SBER_GREEN, SBER_BLUE, SBER_ORANGE

# Получение палитры
colors = nm.get_palette('primary')  # Основная палитра
colors = nm.get_palette('sequential')  # Последовательная
colors = nm.get_palette('diverging')  # Дивергентная

# Получение определенного количества цветов
colors = nm.get_palette('primary', n_colors=10)
```

### Доступные цвета

- **SBER_GREEN** (`#21A038`) - Основной цвет бренда
- **SBER_BLUE** (`#0E67D7`) - Синий
- **SBER_ORANGE** (`#FF6B00`) - Оранжевый
- **SBER_PURPLE** (`#7B4EA5`) - Фиолетовый
- **SBER_YELLOW** (`#FFB800`) - Желтый
- **SBER_RED** (`#E31E24`) - Красный

## 📁 Структура проекта

```
neuro-metrics/
├── src/
│   └── neuro_metrics/
│       ├── __init__.py      # Главный API
│       ├── colors.py        # Цветовая палитра
│       └── plotting.py      # Функции построения графиков
├── pyproject.toml           # Конфигурация проекта (uv)
└── README.md
```

## 💡 Примеры использования

### Пример 1: Анализ продаж

```python
import neuro_metrics as nm
import pandas as pd

# Загрузка данных
sales_data = pd.DataFrame({
    'month': ['Янв', 'Фев', 'Мар', 'Апр', 'Май', 'Июн'],
    'revenue': [1500, 1800, 1650, 2100, 2300, 2200],
    'target': [1600, 1700, 1800, 1900, 2000, 2100]
})

# Визуализация
nm.plot_line(sales_data, x='month', y=['revenue', 'target'],
             title='Выручка vs План на 2024',
             xlabel='Месяц', ylabel='Выручка (тыс. руб)')
```

### Пример 2: Анализ портфеля

```python
portfolio = {
    'Акции': 45,
    'Облигации': 30,
    'Недвижимость': 15,
    'Кэш': 10
}

nm.plot_pie(portfolio, title='Структура портфеля',
            explode=[0.1, 0, 0, 0])
```

### Пример 3: Корреляционный анализ

```python
# Расчет корреляций
corr = df[['price', 'volume', 'returns', 'volatility']].corr()

# Визуализация
nm.plot_heatmap(corr, 
                title='Корреляционная матрица параметров',
                annot=True, fmt='.2f', cmap='RdYlGn')
```

## 🛠 Разработка

### Установка для разработки

```bash
# Клонировать репозиторий
git clone https://github.com/Sin7009/neuro-metrics.git
cd neuro-metrics

# Установить зависимости
uv sync

# Запустить тесты
uv run python test_examples.py
```

### Зависимости

- `matplotlib>=3.5.0` - Базовая библиотека для построения графиков
- `seaborn>=0.12.0` - Расширенные визуализации
- `pandas>=1.3.0` - Работа с данными
- `numpy>=1.21.0` - Численные вычисления
- `scipy>=1.7.0` - Научные вычисления (для KDE в гистограммах)

## 📝 Лицензия

Этот проект является открытым программным обеспечением.

## 🤝 Вклад

Приветствуются любые улучшения! Пожалуйста, создавайте issues и pull requests.

## 📧 Контакты

Для вопросов и предложений создавайте issues в GitHub репозитории.

---

**Made with ❤️ using modern Python tooling (uv) and Sber corporate colors**